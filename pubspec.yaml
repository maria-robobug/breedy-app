name: breedy
description: An app that shows a random dog breed for the user.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^1.1.1
  cupertino_icons: ^0.1.2
  equatable: ^1.0.0
  flutter_bloc: ^2.0.0
  http: ^0.12.0+2
  logger: ^0.7.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
